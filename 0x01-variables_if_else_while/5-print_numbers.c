#include <stdio.h>
/**
 *main - Entry point
 *Return: Always' 0'(Success)  
 */
int main(void)

{
/**
 *   main - Entry point
 *   Return: Always 0 (Success)
 */
		
		int num;

		for (num = 0; num < 10; num++)
			printf("%d", num);
		putchar('\n');
		return (0);
}
